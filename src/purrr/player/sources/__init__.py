"""Proveedores de estaciones de radio en vivo. Cada uno tiene su propia entrada en
el menú lateral (ver ui/sidebar.py y ui/window.py) y se importa directamente desde
su módulo (`from purrr.player.sources import rainwave`, etc.) — no hay agregador
combinado acá."""
